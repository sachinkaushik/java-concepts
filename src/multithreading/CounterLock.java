package multithreading;

/*
    Intrinsic Lock : These are build into every objects in Java
                    you dont see them , but they are there
                    when you use synchronized keyword,
                    you are using these automatic locks

     Explicit : These are more advanced locks, you can control
                 youself using the Lock class, You explicitly say when to lock n unlock
                 its give you more control
 */
public class CounterLock {
}
